# Career Intel — Frontend

React + Vite + Tailwind v4 dashboard. See the top-level `README.md` in the
repo root for full setup and deployment instructions (Vercel).

Quick start:
```bash
npm install
cp .env.example .env   # set VITE_API_URL to your backend
npm run dev
```
